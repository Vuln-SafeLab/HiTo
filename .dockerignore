# 依赖与构建产物(镜像内会重新安装/构建)
node_modules
.next
out

# 版本控制与本地环境
.git
.gitignore
.env
.env.local
.env.production
.env.*
!.env.example

# 运行时产物(用数据卷持久化,不进镜像)
data
public/uploads

# 大文件与文档(构建用不到)
docs/*.pptx
*.log
npm-debug.log*
pnpm-debug.log*

# Docker 自身
Dockerfile
.dockerignore
docker-compose*.yml
